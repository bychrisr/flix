{
  "name": "@flix/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'api scaffolded - choose runtime (Node/Express/Fastify)'",
    "build": "echo 'api build not configured yet'",
    "test": "echo 'api tests not configured yet'",
    "lint": "echo 'api lint not configured yet'",
    "typecheck": "echo 'api typecheck not configured yet'"
  }
}
