# @flix/web

Learner web app runtime for public catalog and lesson playback flows.

## Runtime

- Dev server: `npm run dev --workspace @flix/web`
- API base URL env: `VITE_API_BASE_URL` (default `http://localhost:3001`)
- Default route: `/events/flix-mvp-launch-event`
